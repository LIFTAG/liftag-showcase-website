# Website localization

## Architecture

Nuxt i18n owns the active EN/SK language. `useSiteLocale()` supplies `locale`, the existing manual preference, `setLocale()`, `href(path)` for internal links, and `basePath` for route comparisons. English uses unprefixed URLs, Slovak `/sk`. Stable app/QR/auth/share endpoints keep their paths and receive `lang`. Marketing URLs determine content language; discovery retains its automatic gym/browser default when no explicit preference exists. Only manual choices write the one-year `liftag-language` cookie.

Legacy query redirects run in `server/plugins/site-locale.ts` before Nitro 2’s public request handler, because its static middleware runs before scanned server middleware. The corresponding query conditions in `new_app/vercel.json` run at the Vercel edge before static/ISR delivery. Redirect responses do not cache or save a preference. Anchors remain in client navigation and survive HTTP redirects through browser fragment inheritance.

Locale middleware redirects language changes only. Both `/sk` and `/sk/` render the Slovak homepage with canonical `/sk`; Vercel owns its configured trailing-slash policy. Store localized downloadable documents in `server/assets`, expose their existing URLs through server routes, and explicitly prerender them. A source `public/sk` directory makes Nitro's development static server redirect `/sk` to `/sk/` before application middleware, which loops if that middleware sends it back.

Nuxt generates twins of a single page. Do not create new `pages/sk` templates. Existing Czech legal routes remain independent. Use `siteBasePath` for route checks and `siteLocalePath` in pure code. Keep all query/identity/filter parameters and hashes when switching. Do not rewrite external, asset, API, mailto, or tel URLs.

## Component messages

Each feature owns `new_app/i18n/messages/<feature>.ts`. Export `en` and `sk`; require `sk satisfies typeof en` (or `LocalizedContent<typeof en>` for literal types). Namespace keys by feature; do not use English sentences or numeric extraction IDs as keys. Components import their own messages and use:

```ts
import { en, sk } from '~/i18n/messages/example'
const { t, n, d } = useI18n({ useScope: 'local', messages: { en, sk } })
const { href, locale } = useSiteLocale()
const label = computed(() => t('example.label'))
```

Template: `{{ t('example.label') }}` and `:to="href('/pricing')"`. Local composers inherit the root locale. Do not change their locale or create another cookie. Shared common strings use the global composer. Keep state/IDs/form values separate from translated labels. `t()` uses `{name}` parameters (not double braces). Translate a whole sentence. Use `<i18n-t>` named slots for inline links/emphasis; trusted existing HTML copy can remain `v-html` where it was already used.

Pure/server renderers can call `createMessageTranslator(locale, { en, sk })` with their flat feature messages. This is Vue I18n with the same Slovak plural rules, independent of a Nuxt component or request cookie. Keep translators scoped to the calling operation. Escape literal pipes in UI/SEO messages as `{'|'}` using Vue I18n literal interpolation; bare `|` separates plural forms.

For arrays, `tm` returns compiled message values; resolve leaves with `rt`. Structured long-form content can be typed EN/SK data selected reactively and imported only by its owning route. Do not stringify user data into HTML. Do not copy page templates for each language. `useLiftagSeo` and `useLiftagStructuredData` accept getters; pass a getter when values depend on locale.

The global Slovak plural forms are `one | few | many (fractions) | other`. Three forms support `one | few | other` for integer counts. Use `Intl`/i18n number and date formatting without changing underlying values, units, or calculator math.

Exercise overlays remain English in `content/exercises/<slug>.ts`; complete Slovak counterparts go in `content/exercises/sk/<slug>.ts` with the same `ExerciseOverlay` type and identities. The async loader imports only the selected guide. Do not invent or translate slug values, relatedSlugs, IDs, URLs, measurements, factual claims, or names of brands/people/gyms. Preserve optional fields and array order.

## Slovak writing

Natural Slovak takes precedence over word-for-word English. Consumer copy uses direct singular address; business copy stays professional, legal wording faithful. Keep sentences short without omitting meaning. Use correct diacritics and grammatical cases. Terminology: workout = tréning; exercise = cvik; set = séria; reps = opakovania; personal record/PR = osobný rekord; workout tracking = zaznamenávanie tréningov; progress = pokrok; routine = tréningová zostava (or simply tréning where clear); plan = tréningový plán; workout logger = tréningový denník; Dashboard remains Dashboard for the trainer/gym management app; use prehľad only for an actual summary view; gym = fitko in casual consumer UI, fitness centrum when the business/legal context calls for it. Prefer "zaznamenať sériu" over "logovať set". Rephrase around external names when their grammatical case cannot be inferred. Preserve standard technical names NFC, QR, RPE, 1RM and product/brand names. Established English gym terms (bench press, hip thrust, superset, etc.) are welcome when they sound natural in Slovak; do not force awkward substitutes. Translate sentence grammar and meaning, never replace individual English words mechanically.

Translate all visible text, aria/alt/title/placeholder text, empty/error/loading states, dynamic chart/canvas labels and metadata. Existing screenshots, recordings, logos and static artwork stay unchanged. Backend content stays API-owned: request its locale and honor provided fallback. Video selection is requested locale → English → available, with displayOrder inside a locale. Shared HLS audio language must update even if the source URL remains identical.

Use native HLS only when the video exposes `audioTracks` as well as supporting the MIME type. Chromium can advertise native HLS without track controls; use HLS.js there. The shared audio binding mirrors the app's requested-language → English preference, normalizes ISO language aliases, reacts to track-list/active-track changes, and flushes buffered old-language speech when HLS.js switches. Unknown language metadata remains untouched.

Public catalog requests use `Accept-Language: en|sk`, matching the mobile app. Server snapshot cache keys include both the API base URL and locale. Keep the API source as a cached-function argument: resolving it only inside a function keyed by locale lets a local fixture overwrite the live catalog in shared Nitro disk storage.

## Agent ownership and verification

Only explicitly configured `gpt-5.6-luna` subagents, at most three at once. Never substitute another model. Each owns its assigned paths and uniquely named message files. Coordinator owns shared route/locale/SEO infrastructure, app/config, dependencies and lockfile, and final integration. Do not revert another agent's edits or commit/push on your own. Report coverage and remaining gaps.

Use `./node_modules/.bin/nuxt typecheck` from `new_app` during work (the host's root pnpm shim tries a different pnpm version). Use Node's existing tests for meaningful behavior. Do not add implementation-mirroring tests for individual translated labels. The coordinator runs the final production SSR and browser suites after integration. Do not submit real contact forms or alter backend/app/dashboard repositories.


## Acceptance checks

`VERCEL_ENV=preview pnpm verify` runs unit checks (including message/guide coverage), typechecking and the default deployment build. Also build with `NITRO_PRESET=node-server` and `NITRO_PRESET=vercel`.

For reproducible API and browser checks, start `node --experimental-strip-types tests/fixtures/discovery-server.mjs` from `new_app`, then run the app with `NUXT_PUBLIC_API_BASE_URL=http://127.0.0.1:4015` on port 3002. Run `node --experimental-strip-types tests/discovery.integration.mjs`, `node tests/siteLocale.integration.mjs`, and `pnpm test:browser` against that server (`DISCOVERY_TEST_ORIGIN` overrides its URL). Install engines once with `pnpm exec playwright install chromium webkit`. The browser suite uses isolated contexts for Chromium/WebKit desktop and mobile, never existing account sessions. Turnstile rejects localhost; tests do not submit forms.

Run production builds and fixture servers in a separate checkout when a development server is already running; Nuxt's generated build files are shared within a checkout. Catalog source-aware keys independently prevent fixture/live cache collisions.

Synthetic catalog video URLs test metadata selection and missing-media behavior; native/HLS.js audio selection and listener cleanup have focused unit coverage. Availability and playback of deployed localized backend videos remain a separate production check.

## Branch history

PR #79 was squash-merged into `main` as `46a7516c4e9c2570ccd6c537739c3abf13898994`. Only the ten localization commits after the original boundary `ebc9d3346adb280deb32cd9d32f04fb4f9651c2a` were rebased onto that commit. The discovery history is not replayed. The rebase retains the newer contextual exercise error handling and mobile video controls from #79; its regression harnesses now include the locale dependencies and verify old-media cleanup before localized playback resumes.

## Verified delivery

- After rebasing onto merged `main` on September 15, `VERCEL_ENV=preview NITRO_PRESET=node-server pnpm verify` passed all **643 tests**, TypeScript and the Node production build. The Vercel production build and both production SSR integration suites also passed. The full Chromium/WebKit desktop/mobile suite passed **42 checks**, with **2 intentional mobile app-store skips**. Verification ran in an isolated checkout; the existing production-only limitations below remain.

- `VERCEL_ENV=preview NITRO_PRESET=node-server pnpm verify`: 621 tests, TypeScript and production build passed.
- `VERCEL_ENV=preview NITRO_PRESET=vercel pnpm build`: passed; the GitHub Vercel deployment check also passed.
- Production fixture runs of `tests/discovery.integration.mjs` and `tests/siteLocale.integration.mjs`: passed. The locale suite covers every static sitemap destination, initial HTML and FAQ text, canonicals/alternates, cookie isolation, legacy redirects, stable handoffs, Czech legal routes and localized 404s.
- Chromium and WebKit, desktop and mobile: 38 passed, 2 intentional mobile app-store navigation skips. Coverage includes pending discovery input, query/filter/identity preservation, keyboard switching, longer copy, form labels, marketing/comparisons, missing video recovery, normal animation and reduced motion.
- All 31 prerendered Slovak HTML pages have matching language, canonical/alternate links and translated titles. Key/interpolation parity covers every feature, all 109 guides and all 11 journal articles. A template AST audit found only intentional brands, URLs and contact addresses as literal text.
- Final calculator and hero corrections passed the full 621-test/typecheck verification, both production build presets, both SSR integration suites and 8 focused Chromium/WebKit desktop/mobile checks. These verify translated validation and draft/share-parameter retention during language switching, hero colors, loaded-font text bounds and visible calls to action. Additional headline checks passed at 320, 360, 390, 412, 700 and 768px. The broader 38-test browser run above covers the preceding implementation commit.
- Catalog cache follow-up: `VERCEL_ENV=preview NITRO_PRESET=vercel pnpm verify` passed all 622 tests, typechecking and the production build. The new regression covers source/language isolation, pagination, media retention and cache reuse. Live EN/SK catalog responses contain the same 469 exercise IDs and 469 image URLs; 446 names differ between languages, with backend-provided names preserved throughout.
- The restored live catalog passed 8 Chromium/WebKit desktop/mobile EN/SK checks: correct HTML language, 469 total records, 48 initial tiles, successfully loaded real images and no uncaught page errors.
- Navigation follow-up: Exercises/Cviky and Demo/Ukážka exchange positions in both menus. The green hover copy is explicitly hidden after its return animation. Chromium/WebKit EN/SK pixel comparisons pass at 1×/2× scale with both site and fallback fonts, including hover and keyboard-focus return; reduced-motion behavior and mobile order/destinations also pass.
- Homepage redirect follow-up: all 622 tests, typechecking, Node/Vercel production builds and both SSR integration suites passed in an isolated checkout. Nine dev requests cover `/sk`, `/sk/` and the legacy query with absent/EN/SK cookies. The new logo regression passed in Chromium/WebKit desktop/mobile against both dev and production (8 passes), including the saved Slovak preference and query/anchor retention. The Markdown URL returns the original content, and both deployment presets prerender it.
- Audio follow-up: `VERCEL_ENV=preview NITRO_PRESET=vercel pnpm verify` passed all 626 tests, typechecking and the build in an isolated checkout. The live lat-pulldown detail player plays SK/EN successfully in Chromium (matching HLS.js audio requests) and WebKit (matching enabled native track). A separate browser harness using the actual shared binding and live stream passed EN↔SK switching on the same playing video in both engines, desktop/mobile, without replacing its URL or resetting playback. Page navigation can still remount a player; this verifies its audio binding, not playback continuity across routes.

Earlier anonymous API probes returned HTTP 403; the subsequent live public catalog check returned HTTP 200 in both languages and verified all 469 exercise records and image URLs. Representative live video/audio playback is verified above; this does not establish translation or media completeness across the entire catalog. Physical-device audio playback remains to be checked. Turnstile submissions and physical-device/app-store handoffs were not performed. Headless mobile WebKit shows the same glow rasterization artifact in English and Slovak; physical Safari rendering needs confirmation. Local redirect checks and deployment builds pass, but authenticated preview access was unavailable for an independent CDN HTTP check.
